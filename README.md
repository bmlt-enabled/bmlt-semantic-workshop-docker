# BMLT Semantic Workshop Docker Image

To run this call out:

```shell
docker run -d -p 8081:80 radius314/bmlt-semantic-workshop:latest
```

Then in your browser navigate to `http://localhost:8081/index.php`
